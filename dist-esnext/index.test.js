import { ok } from 'ptz-assert';
import * as index from './index';
describe('ptz-core-domain', () => {
    describe('exports', () => {
        it('generateId', () => ok(index.generateId));
        it('generateIdWith', () => ok(index.generateIdWith));
    });
});
//# sourceMappingURL=index.test.js.map